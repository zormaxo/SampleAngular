import { BigsortDirective } from './bigsort.directive';

describe('BigsortDirective', () => {
  it('should create an instance', () => {
    const directive = new BigsortDirective();
    expect(directive).toBeTruthy();
  });
});
